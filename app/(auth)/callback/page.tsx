import AuthCallbackContainer from '@/components/AuthCallback/AuthCallbackContainer';

// 구글에서 code를 받아온 뒤 리다이렉트되는 페이지
export default function AuthCallbackPage() {
  return <AuthCallbackContainer />;
}
